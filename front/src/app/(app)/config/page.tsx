import { Placeholder } from "@/components/placeholder";

export default function ConfigPage() {
  return (
    <Placeholder
      title="Configuração"
      phase="C8"
      note="Ramos, cidade base, raio, serviço padrão, piloto automático e conectar Google Calendar. Entra na C8 (e serve de onboarding)."
    />
  );
}
